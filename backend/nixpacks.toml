providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[start]
cmd = "chmod +x /root/.local/bin/uvicorn && /root/.local/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
